// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: benefit.sql

package db

import (
	"context"

	uuid "github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createBenefit = `-- name: CreateBenefit :one
INSERT INTO benefits (plan_id, name, category, annual_limit, co_pay_type, co_pay_value, waiting_period_days)
VALUES ($1, $2, $3, $4, $5, $6, $7) RETURNING id, plan_id, name, category, annual_limit, co_pay_type, co_pay_value, waiting_period_days, created_at, updated_at
`

type CreateBenefitParams struct {
	PlanID            uuid.UUID `json:"plan_id"`
	Name              string    `json:"name"`
	Category          string    `json:"category"`
	AnnualLimit       int64     `json:"annual_limit"`
	CoPayType         string    `json:"co_pay_type"`
	CoPayValue        int64     `json:"co_pay_value"`
	WaitingPeriodDays int32     `json:"waiting_period_days"`
}

func (q *Queries) CreateBenefit(ctx context.Context, arg CreateBenefitParams) (Benefit, error) {
	row := q.db.QueryRow(ctx, createBenefit,
		arg.PlanID,
		arg.Name,
		arg.Category,
		arg.AnnualLimit,
		arg.CoPayType,
		arg.CoPayValue,
		arg.WaitingPeriodDays,
	)
	var i Benefit
	err := row.Scan(
		&i.ID,
		&i.PlanID,
		&i.Name,
		&i.Category,
		&i.AnnualLimit,
		&i.CoPayType,
		&i.CoPayValue,
		&i.WaitingPeriodDays,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteBenefit = `-- name: DeleteBenefit :exec
DELETE FROM benefits WHERE id = $1
`

func (q *Queries) DeleteBenefit(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteBenefit, id)
	return err
}

const getBenefitByID = `-- name: GetBenefitByID :one
SELECT id, plan_id, name, category, annual_limit, co_pay_type, co_pay_value, waiting_period_days, created_at, updated_at FROM benefits WHERE id = $1
`

func (q *Queries) GetBenefitByID(ctx context.Context, id uuid.UUID) (Benefit, error) {
	row := q.db.QueryRow(ctx, getBenefitByID, id)
	var i Benefit
	err := row.Scan(
		&i.ID,
		&i.PlanID,
		&i.Name,
		&i.Category,
		&i.AnnualLimit,
		&i.CoPayType,
		&i.CoPayValue,
		&i.WaitingPeriodDays,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listBenefitsByCategory = `-- name: ListBenefitsByCategory :many
SELECT id, plan_id, name, category, annual_limit, co_pay_type, co_pay_value, waiting_period_days, created_at, updated_at FROM benefits WHERE plan_id = $1 AND category = $2
`

type ListBenefitsByCategoryParams struct {
	PlanID   uuid.UUID `json:"plan_id"`
	Category string    `json:"category"`
}

func (q *Queries) ListBenefitsByCategory(ctx context.Context, arg ListBenefitsByCategoryParams) ([]Benefit, error) {
	rows, err := q.db.Query(ctx, listBenefitsByCategory, arg.PlanID, arg.Category)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Benefit{}
	for rows.Next() {
		var i Benefit
		if err := rows.Scan(
			&i.ID,
			&i.PlanID,
			&i.Name,
			&i.Category,
			&i.AnnualLimit,
			&i.CoPayType,
			&i.CoPayValue,
			&i.WaitingPeriodDays,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listBenefitsByPlan = `-- name: ListBenefitsByPlan :many
SELECT id, plan_id, name, category, annual_limit, co_pay_type, co_pay_value, waiting_period_days, created_at, updated_at FROM benefits WHERE plan_id = $1 ORDER BY category, name
`

func (q *Queries) ListBenefitsByPlan(ctx context.Context, planID uuid.UUID) ([]Benefit, error) {
	rows, err := q.db.Query(ctx, listBenefitsByPlan, planID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Benefit{}
	for rows.Next() {
		var i Benefit
		if err := rows.Scan(
			&i.ID,
			&i.PlanID,
			&i.Name,
			&i.Category,
			&i.AnnualLimit,
			&i.CoPayType,
			&i.CoPayValue,
			&i.WaitingPeriodDays,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateBenefit = `-- name: UpdateBenefit :one
UPDATE benefits SET
    name = COALESCE($1, name),
    category = COALESCE($2, category),
    annual_limit = COALESCE($3, annual_limit),
    co_pay_type = COALESCE($4, co_pay_type),
    co_pay_value = COALESCE($5, co_pay_value),
    waiting_period_days = COALESCE($6, waiting_period_days),
    updated_at = NOW()
WHERE id = $7 RETURNING id, plan_id, name, category, annual_limit, co_pay_type, co_pay_value, waiting_period_days, created_at, updated_at
`

type UpdateBenefitParams struct {
	Name              pgtype.Text `json:"name"`
	Category          pgtype.Text `json:"category"`
	AnnualLimit       pgtype.Int8 `json:"annual_limit"`
	CoPayType         pgtype.Text `json:"co_pay_type"`
	CoPayValue        pgtype.Int8 `json:"co_pay_value"`
	WaitingPeriodDays pgtype.Int4 `json:"waiting_period_days"`
	ID                uuid.UUID   `json:"id"`
}

func (q *Queries) UpdateBenefit(ctx context.Context, arg UpdateBenefitParams) (Benefit, error) {
	row := q.db.QueryRow(ctx, updateBenefit,
		arg.Name,
		arg.Category,
		arg.AnnualLimit,
		arg.CoPayType,
		arg.CoPayValue,
		arg.WaitingPeriodDays,
		arg.ID,
	)
	var i Benefit
	err := row.Scan(
		&i.ID,
		&i.PlanID,
		&i.Name,
		&i.Category,
		&i.AnnualLimit,
		&i.CoPayType,
		&i.CoPayValue,
		&i.WaitingPeriodDays,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
