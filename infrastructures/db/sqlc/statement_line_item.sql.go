// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: statement_line_item.sql

package db

import (
	"context"

	uuid "github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createStatementLineItem = `-- name: CreateStatementLineItem :one
INSERT INTO statement_line_items (statement_id, claim_number, service_date, member_name, procedure_code, claimed_amount, match_status)
VALUES ($1, $2, $3, $4, $5, $6, $7) RETURNING id, statement_id, claim_number, service_date, member_name, procedure_code, claimed_amount, matched_claim_id, match_status, discrepancy_amount, notes, created_at
`

type CreateStatementLineItemParams struct {
	StatementID   uuid.UUID   `json:"statement_id"`
	ClaimNumber   pgtype.Text `json:"claim_number"`
	ServiceDate   pgtype.Date `json:"service_date"`
	MemberName    pgtype.Text `json:"member_name"`
	ProcedureCode pgtype.Text `json:"procedure_code"`
	ClaimedAmount int64       `json:"claimed_amount"`
	MatchStatus   string      `json:"match_status"`
}

func (q *Queries) CreateStatementLineItem(ctx context.Context, arg CreateStatementLineItemParams) (StatementLineItem, error) {
	row := q.db.QueryRow(ctx, createStatementLineItem,
		arg.StatementID,
		arg.ClaimNumber,
		arg.ServiceDate,
		arg.MemberName,
		arg.ProcedureCode,
		arg.ClaimedAmount,
		arg.MatchStatus,
	)
	var i StatementLineItem
	err := row.Scan(
		&i.ID,
		&i.StatementID,
		&i.ClaimNumber,
		&i.ServiceDate,
		&i.MemberName,
		&i.ProcedureCode,
		&i.ClaimedAmount,
		&i.MatchedClaimID,
		&i.MatchStatus,
		&i.DiscrepancyAmount,
		&i.Notes,
		&i.CreatedAt,
	)
	return i, err
}

const listStatementLineItemsByStatement = `-- name: ListStatementLineItemsByStatement :many
SELECT id, statement_id, claim_number, service_date, member_name, procedure_code, claimed_amount, matched_claim_id, match_status, discrepancy_amount, notes, created_at FROM statement_line_items WHERE statement_id = $1 ORDER BY created_at
`

func (q *Queries) ListStatementLineItemsByStatement(ctx context.Context, statementID uuid.UUID) ([]StatementLineItem, error) {
	rows, err := q.db.Query(ctx, listStatementLineItemsByStatement, statementID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []StatementLineItem{}
	for rows.Next() {
		var i StatementLineItem
		if err := rows.Scan(
			&i.ID,
			&i.StatementID,
			&i.ClaimNumber,
			&i.ServiceDate,
			&i.MemberName,
			&i.ProcedureCode,
			&i.ClaimedAmount,
			&i.MatchedClaimID,
			&i.MatchStatus,
			&i.DiscrepancyAmount,
			&i.Notes,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const matchStatementLineItem = `-- name: MatchStatementLineItem :one
UPDATE statement_line_items SET
    matched_claim_id = $2,
    match_status = $3,
    discrepancy_amount = $4,
    notes = $5
WHERE id = $1 RETURNING id, statement_id, claim_number, service_date, member_name, procedure_code, claimed_amount, matched_claim_id, match_status, discrepancy_amount, notes, created_at
`

type MatchStatementLineItemParams struct {
	ID                uuid.UUID   `json:"id"`
	MatchedClaimID    pgtype.UUID `json:"matched_claim_id"`
	MatchStatus       string      `json:"match_status"`
	DiscrepancyAmount int64       `json:"discrepancy_amount"`
	Notes             pgtype.Text `json:"notes"`
}

func (q *Queries) MatchStatementLineItem(ctx context.Context, arg MatchStatementLineItemParams) (StatementLineItem, error) {
	row := q.db.QueryRow(ctx, matchStatementLineItem,
		arg.ID,
		arg.MatchedClaimID,
		arg.MatchStatus,
		arg.DiscrepancyAmount,
		arg.Notes,
	)
	var i StatementLineItem
	err := row.Scan(
		&i.ID,
		&i.StatementID,
		&i.ClaimNumber,
		&i.ServiceDate,
		&i.MemberName,
		&i.ProcedureCode,
		&i.ClaimedAmount,
		&i.MatchedClaimID,
		&i.MatchStatus,
		&i.DiscrepancyAmount,
		&i.Notes,
		&i.CreatedAt,
	)
	return i, err
}
