// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: treaty_participant.sql

package db

import (
	"context"

	uuid "github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createTreatyParticipant = `-- name: CreateTreatyParticipant :one
INSERT INTO treaty_participants (treaty_id, reinsurer_name, share_percentage, commission_rate, is_lead)
VALUES ($1, $2, $3, $4, $5) RETURNING id, treaty_id, reinsurer_name, share_percentage, commission_rate, is_lead, created_at, updated_at
`

type CreateTreatyParticipantParams struct {
	TreatyID        uuid.UUID      `json:"treaty_id"`
	ReinsurerName   string         `json:"reinsurer_name"`
	SharePercentage pgtype.Numeric `json:"share_percentage"`
	CommissionRate  pgtype.Numeric `json:"commission_rate"`
	IsLead          bool           `json:"is_lead"`
}

func (q *Queries) CreateTreatyParticipant(ctx context.Context, arg CreateTreatyParticipantParams) (TreatyParticipant, error) {
	row := q.db.QueryRow(ctx, createTreatyParticipant,
		arg.TreatyID,
		arg.ReinsurerName,
		arg.SharePercentage,
		arg.CommissionRate,
		arg.IsLead,
	)
	var i TreatyParticipant
	err := row.Scan(
		&i.ID,
		&i.TreatyID,
		&i.ReinsurerName,
		&i.SharePercentage,
		&i.CommissionRate,
		&i.IsLead,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteTreatyParticipant = `-- name: DeleteTreatyParticipant :exec
DELETE FROM treaty_participants WHERE id = $1
`

func (q *Queries) DeleteTreatyParticipant(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteTreatyParticipant, id)
	return err
}

const getTotalShareByTreaty = `-- name: GetTotalShareByTreaty :one
SELECT COALESCE(SUM(share_percentage), 0)::numeric(5,2) as total_share
FROM treaty_participants WHERE treaty_id = $1
`

func (q *Queries) GetTotalShareByTreaty(ctx context.Context, treatyID uuid.UUID) (pgtype.Numeric, error) {
	row := q.db.QueryRow(ctx, getTotalShareByTreaty, treatyID)
	var total_share pgtype.Numeric
	err := row.Scan(&total_share)
	return total_share, err
}

const getTreatyParticipantByID = `-- name: GetTreatyParticipantByID :one
SELECT id, treaty_id, reinsurer_name, share_percentage, commission_rate, is_lead, created_at, updated_at FROM treaty_participants WHERE id = $1
`

func (q *Queries) GetTreatyParticipantByID(ctx context.Context, id uuid.UUID) (TreatyParticipant, error) {
	row := q.db.QueryRow(ctx, getTreatyParticipantByID, id)
	var i TreatyParticipant
	err := row.Scan(
		&i.ID,
		&i.TreatyID,
		&i.ReinsurerName,
		&i.SharePercentage,
		&i.CommissionRate,
		&i.IsLead,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listTreatyParticipantsByTreaty = `-- name: ListTreatyParticipantsByTreaty :many
SELECT id, treaty_id, reinsurer_name, share_percentage, commission_rate, is_lead, created_at, updated_at FROM treaty_participants WHERE treaty_id = $1 ORDER BY is_lead DESC, reinsurer_name ASC
`

func (q *Queries) ListTreatyParticipantsByTreaty(ctx context.Context, treatyID uuid.UUID) ([]TreatyParticipant, error) {
	rows, err := q.db.Query(ctx, listTreatyParticipantsByTreaty, treatyID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []TreatyParticipant{}
	for rows.Next() {
		var i TreatyParticipant
		if err := rows.Scan(
			&i.ID,
			&i.TreatyID,
			&i.ReinsurerName,
			&i.SharePercentage,
			&i.CommissionRate,
			&i.IsLead,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTreatyParticipant = `-- name: UpdateTreatyParticipant :one
UPDATE treaty_participants SET
    reinsurer_name = $2, share_percentage = $3, commission_rate = $4, is_lead = $5, updated_at = NOW()
WHERE id = $1 RETURNING id, treaty_id, reinsurer_name, share_percentage, commission_rate, is_lead, created_at, updated_at
`

type UpdateTreatyParticipantParams struct {
	ID              uuid.UUID      `json:"id"`
	ReinsurerName   string         `json:"reinsurer_name"`
	SharePercentage pgtype.Numeric `json:"share_percentage"`
	CommissionRate  pgtype.Numeric `json:"commission_rate"`
	IsLead          bool           `json:"is_lead"`
}

func (q *Queries) UpdateTreatyParticipant(ctx context.Context, arg UpdateTreatyParticipantParams) (TreatyParticipant, error) {
	row := q.db.QueryRow(ctx, updateTreatyParticipant,
		arg.ID,
		arg.ReinsurerName,
		arg.SharePercentage,
		arg.CommissionRate,
		arg.IsLead,
	)
	var i TreatyParticipant
	err := row.Scan(
		&i.ID,
		&i.TreatyID,
		&i.ReinsurerName,
		&i.SharePercentage,
		&i.CommissionRate,
		&i.IsLead,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
