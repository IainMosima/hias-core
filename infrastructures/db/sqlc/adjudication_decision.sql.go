// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: adjudication_decision.sql

package db

import (
	"context"
	"encoding/json"
	"time"

	uuid "github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createAdjudicationDecision = `-- name: CreateAdjudicationDecision :one
INSERT INTO adjudication_decisions (claim_id, decision, payable_amount, member_responsibility, reasons, rule_results, adjudicated_by, adjudicated_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8) RETURNING id, claim_id, decision, payable_amount, member_responsibility, reasons, rule_results, adjudicated_by, adjudicated_at, created_at, updated_at
`

type CreateAdjudicationDecisionParams struct {
	ClaimID              uuid.UUID       `json:"claim_id"`
	Decision             string          `json:"decision"`
	PayableAmount        int64           `json:"payable_amount"`
	MemberResponsibility int64           `json:"member_responsibility"`
	Reasons              json.RawMessage `json:"reasons"`
	RuleResults          json.RawMessage `json:"rule_results"`
	AdjudicatedBy        pgtype.UUID     `json:"adjudicated_by"`
	AdjudicatedAt        time.Time       `json:"adjudicated_at"`
}

func (q *Queries) CreateAdjudicationDecision(ctx context.Context, arg CreateAdjudicationDecisionParams) (AdjudicationDecision, error) {
	row := q.db.QueryRow(ctx, createAdjudicationDecision,
		arg.ClaimID,
		arg.Decision,
		arg.PayableAmount,
		arg.MemberResponsibility,
		arg.Reasons,
		arg.RuleResults,
		arg.AdjudicatedBy,
		arg.AdjudicatedAt,
	)
	var i AdjudicationDecision
	err := row.Scan(
		&i.ID,
		&i.ClaimID,
		&i.Decision,
		&i.PayableAmount,
		&i.MemberResponsibility,
		&i.Reasons,
		&i.RuleResults,
		&i.AdjudicatedBy,
		&i.AdjudicatedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getAdjudicationByClaimID = `-- name: GetAdjudicationByClaimID :one
SELECT id, claim_id, decision, payable_amount, member_responsibility, reasons, rule_results, adjudicated_by, adjudicated_at, created_at, updated_at FROM adjudication_decisions WHERE claim_id = $1 ORDER BY created_at DESC LIMIT 1
`

func (q *Queries) GetAdjudicationByClaimID(ctx context.Context, claimID uuid.UUID) (AdjudicationDecision, error) {
	row := q.db.QueryRow(ctx, getAdjudicationByClaimID, claimID)
	var i AdjudicationDecision
	err := row.Scan(
		&i.ID,
		&i.ClaimID,
		&i.Decision,
		&i.PayableAmount,
		&i.MemberResponsibility,
		&i.Reasons,
		&i.RuleResults,
		&i.AdjudicatedBy,
		&i.AdjudicatedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listAdjudicationsByDecision = `-- name: ListAdjudicationsByDecision :many
SELECT id, claim_id, decision, payable_amount, member_responsibility, reasons, rule_results, adjudicated_by, adjudicated_at, created_at, updated_at FROM adjudication_decisions WHERE decision = $1 ORDER BY created_at DESC LIMIT $2 OFFSET $3
`

type ListAdjudicationsByDecisionParams struct {
	Decision string `json:"decision"`
	Limit    int32  `json:"limit"`
	Offset   int32  `json:"offset"`
}

func (q *Queries) ListAdjudicationsByDecision(ctx context.Context, arg ListAdjudicationsByDecisionParams) ([]AdjudicationDecision, error) {
	rows, err := q.db.Query(ctx, listAdjudicationsByDecision, arg.Decision, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AdjudicationDecision{}
	for rows.Next() {
		var i AdjudicationDecision
		if err := rows.Scan(
			&i.ID,
			&i.ClaimID,
			&i.Decision,
			&i.PayableAmount,
			&i.MemberResponsibility,
			&i.Reasons,
			&i.RuleResults,
			&i.AdjudicatedBy,
			&i.AdjudicatedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
