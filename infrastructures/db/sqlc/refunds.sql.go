// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: refunds.sql

package db

import (
	"context"

	uuid "github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const approveRefund = `-- name: ApproveRefund :one
UPDATE refunds SET status = 'APPROVED', approved_by = $2, approved_at = NOW(), updated_at = NOW()
WHERE id = $1 RETURNING id, policy_id, credit_note_id, amount, currency, status, reason, approved_by, approved_at, processed_at, created_by, created_at, updated_at
`

type ApproveRefundParams struct {
	ID         uuid.UUID   `json:"id"`
	ApprovedBy pgtype.UUID `json:"approved_by"`
}

func (q *Queries) ApproveRefund(ctx context.Context, arg ApproveRefundParams) (Refund, error) {
	row := q.db.QueryRow(ctx, approveRefund, arg.ID, arg.ApprovedBy)
	var i Refund
	err := row.Scan(
		&i.ID,
		&i.PolicyID,
		&i.CreditNoteID,
		&i.Amount,
		&i.Currency,
		&i.Status,
		&i.Reason,
		&i.ApprovedBy,
		&i.ApprovedAt,
		&i.ProcessedAt,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const createRefund = `-- name: CreateRefund :one
INSERT INTO refunds (policy_id, credit_note_id, amount, currency, status, reason, created_by)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, policy_id, credit_note_id, amount, currency, status, reason, approved_by, approved_at, processed_at, created_by, created_at, updated_at
`

type CreateRefundParams struct {
	PolicyID     uuid.UUID   `json:"policy_id"`
	CreditNoteID pgtype.UUID `json:"credit_note_id"`
	Amount       int64       `json:"amount"`
	Currency     string      `json:"currency"`
	Status       string      `json:"status"`
	Reason       string      `json:"reason"`
	CreatedBy    pgtype.UUID `json:"created_by"`
}

func (q *Queries) CreateRefund(ctx context.Context, arg CreateRefundParams) (Refund, error) {
	row := q.db.QueryRow(ctx, createRefund,
		arg.PolicyID,
		arg.CreditNoteID,
		arg.Amount,
		arg.Currency,
		arg.Status,
		arg.Reason,
		arg.CreatedBy,
	)
	var i Refund
	err := row.Scan(
		&i.ID,
		&i.PolicyID,
		&i.CreditNoteID,
		&i.Amount,
		&i.Currency,
		&i.Status,
		&i.Reason,
		&i.ApprovedBy,
		&i.ApprovedAt,
		&i.ProcessedAt,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getRefundByID = `-- name: GetRefundByID :one
SELECT id, policy_id, credit_note_id, amount, currency, status, reason, approved_by, approved_at, processed_at, created_by, created_at, updated_at FROM refunds WHERE id = $1
`

func (q *Queries) GetRefundByID(ctx context.Context, id uuid.UUID) (Refund, error) {
	row := q.db.QueryRow(ctx, getRefundByID, id)
	var i Refund
	err := row.Scan(
		&i.ID,
		&i.PolicyID,
		&i.CreditNoteID,
		&i.Amount,
		&i.Currency,
		&i.Status,
		&i.Reason,
		&i.ApprovedBy,
		&i.ApprovedAt,
		&i.ProcessedAt,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listRefundsByPolicy = `-- name: ListRefundsByPolicy :many
SELECT id, policy_id, credit_note_id, amount, currency, status, reason, approved_by, approved_at, processed_at, created_by, created_at, updated_at FROM refunds WHERE policy_id = $1 ORDER BY created_at DESC LIMIT $2 OFFSET $3
`

type ListRefundsByPolicyParams struct {
	PolicyID uuid.UUID `json:"policy_id"`
	Limit    int32     `json:"limit"`
	Offset   int32     `json:"offset"`
}

func (q *Queries) ListRefundsByPolicy(ctx context.Context, arg ListRefundsByPolicyParams) ([]Refund, error) {
	rows, err := q.db.Query(ctx, listRefundsByPolicy, arg.PolicyID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Refund{}
	for rows.Next() {
		var i Refund
		if err := rows.Scan(
			&i.ID,
			&i.PolicyID,
			&i.CreditNoteID,
			&i.Amount,
			&i.Currency,
			&i.Status,
			&i.Reason,
			&i.ApprovedBy,
			&i.ApprovedAt,
			&i.ProcessedAt,
			&i.CreatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const processRefund = `-- name: ProcessRefund :one
UPDATE refunds SET status = 'PROCESSED', processed_at = NOW(), updated_at = NOW()
WHERE id = $1 RETURNING id, policy_id, credit_note_id, amount, currency, status, reason, approved_by, approved_at, processed_at, created_by, created_at, updated_at
`

func (q *Queries) ProcessRefund(ctx context.Context, id uuid.UUID) (Refund, error) {
	row := q.db.QueryRow(ctx, processRefund, id)
	var i Refund
	err := row.Scan(
		&i.ID,
		&i.PolicyID,
		&i.CreditNoteID,
		&i.Amount,
		&i.Currency,
		&i.Status,
		&i.Reason,
		&i.ApprovedBy,
		&i.ApprovedAt,
		&i.ProcessedAt,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
